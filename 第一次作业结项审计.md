# 第一次作业结项审计

- 审计日期：2026-09-26
- 项目：SysY 简化编译器实现
- 阶段：第一次作业
- 目标架构：RISC-V 64 位
- 工具链：SiFive GCC 10.2 + SiFive binutils 2.35
- 审计范围：基础要求第一部分、基础要求第二部分、进阶要求、报告与提交

## 一、总体结论

实验代码、测试和 Markdown 版完整实验报告已经完成。

当前不能宣称“作业全部提交完成”，原因是：

1. 仓库中还没有从 Markdown 报告导出的最终 PDF；
2. 无法从仓库判断两名成员是否已经在小雅平台分别提交；
3. 投稿前仍需要完成 Markdown 到 PDF 的排版和格式检查。

因此当前状态应表述为：

```text
实验内容：完成
代码与测试：完成
进阶探索：完成
Markdown 报告：完成
最终 PDF：未完成
小雅提交：需要在平台完成，仓库无法验证
```

## 二、基础要求第一部分审计

| 检查项 | 状态 | 证据 |
| --- | --- | --- |
| 编写简单 C 程序 | 完成 | `part1_gcc/fib.c` |
| 预处理阶段 | 完成 | `artifacts/fib.i` |
| 编译器阶段 | 完成 | `fib.c.original`、`fib.c.gimple`、`fib.c.rtl-*` |
| RISC-V 汇编生成 | 完成 | `artifacts/fib.s` |
| `-O2` 对照 | 完成 | `artifacts/fib.O2.s` |
| 汇编器阶段 | 完成 | `artifacts/fib.o` |
| 链接器阶段 | 完成 | `artifacts/fib` |
| 运行库处理 | 完成 | SiFive 重新构建 `libsysy_riscv_sifive.a` |
| QEMU 验证 | 完成 | 输入 `0、1、5、10` 全部通过 |
| 分析文档 | 完成 | `part1_gcc/阶段分析.md` |

自动复现：

```bash
cd first_assignment/part1_gcc
make -B all verify inspect environment
```

结果：

```text
PASS n=0
PASS n=1
PASS n=5
PASS n=10
```

## 三、基础要求第二部分审计

| 检查项 | 状态 | 证据 |
| --- | --- | --- |
| 设计 SysY 示例程序 | 完成 | `sysy_demo.sy` |
| 覆盖常量、函数、分支、循环 | 完成 | `is_even`、`gcd`、`main` |
| 覆盖 `break/continue` | 完成 | `main` |
| 编写 LLVM IR | 完成 | `sysy_demo.ll` |
| LLVM IR 链接运行库 | 完成 | SiFive 重建运行库 |
| LLVM IR 生成 RISC-V 程序 | 完成 | `llc -> as -> ld` |
| 手写 RISC-V 汇编 | 完成 | `sysy_demo_asm.s` |
| 手写汇编链接运行库 | 完成 | `libsysy_riscv_sifive.a` |
| LLVM IR 路线运行验证 | 完成 | `artifacts/tests/` |
| 手写汇编路线运行验证 | 完成 | `artifacts/tests_asm/` |
| 两路线结果对照 | 完成 | 6 组输出一致 |

自动复现：

```bash
cd first_assignment/part2_sysy_llvm_riscv
make -B all verify inspect asm-inspect environment
```

结果：

```text
LLVM IR: PASS n=0,1,3,4,5,10
手写汇编: PASS n=0,1,3,4,5,10
```

最终 RISC-V 属性：

```text
Tag_RISCV_arch: "rv64i2p0_m2p0_a2p0_f2p0_d2p0_c2p0"
ABI: LP64D
```

## 四、进阶要求审计

| 检查项 | 状态 | 证据 |
| --- | --- | --- |
| 观察 MLIR 多层方言 | 完成 | `MLIR中端_lowering分析.md` |
| 阅读 AscendNPU IR 架构 | 完成 | 官方源码与文档分析 |
| VecAdd 示例 | 完成 | `vecadd.mlir` |
| AIV 核类型推断 | 完成 | `vecadd_hivm-infer-func-core-type.mlir` |
| 片上内存 lowering | 完成 | `vecadd_hivm_core_plan.mlir` |
| 流水同步插入 | 完成 | `vecadd_hivm_sync.mlir` |
| 方言转换 | 完成 | `hivm.hir.vadd -> linalg.add` |
| 完整 lowering Pipeline | 完成 | 30-pass pipeline |
| 与 LLVM IR 对比 | 完成 | `MLIR中端_lowering分析.md` |

自动复现：

```bash
cd first_assignment/advanced_mlir
make -B all
make verify
```

结果：

```text
PASS: MLIR lowering evidence verified
```

说明：

- 本任务只观察编译器 IR 和 lowering；
- 不需要真实 NPU；
- 不需要 CANN；
- 不需要 `hivmc` 和设备上板。

## 五、报告要求审计

作业要求 PDF，并建议包含：

- 题目；
- 摘要；
- 关键词；
- 引言；
- 工作与结果；
- 结论；
- 参考文献；
- 图表和格式规范。

当前已有完整 Markdown 报告：

```text
第一次作业实验报告.md
```

报告已经包含：

- 题目、摘要、关键词和引言；
- 实验分工；
- GCC 完整编译过程；
- SysY、LLVM IR 和手写 RISC-V 汇编；
- MLIR 中端 lowering；
- 结果、问题、结论和参考文献；
- 关键代码片段、实验证据索引和复现命令；完整代码保存在仓库源码文件中。

尚未完成：

```text
Markdown 到最终 PDF 的排版
PDF 目录、页码、浮动图表和引用样式检查
```

## 六、提交要求审计

作业要求：

1. 两人共同完成报告；
2. 报告开头说明分工；
3. 两人均在小雅平台提交报告；
4. 提交 PDF 格式。

目前：

- 分工文件已完成；
- GitHub 代码和证据已提交；
- 小雅平台提交状态无法从本地仓库判断；
- 最终 PDF 尚未生成。

因此必须在小雅平台完成以下动作：

1. 上传同一版本 PDF；
2. 刘国全提交一次；
3. 刘昀皓提交一次；
4. 确认平台显示两人均提交成功。

## 七、结项前必须完成的任务

1. 使用现有 `第一次作业实验报告.md` 完成最终 PDF 排版。
2. 检查标题、摘要、关键词、目录、页码、图表编号和参考文献样式。
3. 两人分别复核最终 PDF。
4. 两人分别在小雅平台提交。
5. 提交后保存回执或截图。

## 八、当前评分判断

代码和实验部分已经满足第一次作业的技术要求。

在教师或助教仅检查仓库内容时，可以认为：

```text
技术任务：完成
可复现实验：完成
Markdown 完整报告：完成
```

但在“课程作业已经圆满完成”的最终口径下，仍需完成：

```text
最终 PDF + 两人小雅提交
```

完成这两项后，第一次作业才可正式判定为全部完成。
