# 编译原理上机实验分工

## 一、项目基本信息

- 课程：编译原理
- 实验主题：SysY 简化编译器实现
- 小组成员：刘国全、刘昀皓
- 当前阶段：第一次上机作业（预备工作 1）
- 目标架构：RISC-V 64 位
- 目标语言：RISC-V 汇编
- 运行环境：WSL2 + Ubuntu 24.04
- 目标运行方式：QEMU 用户态模拟
- 文档版本：V1.0
- 创建日期：2026-09-24

## 二、固定技术路线

本项目选定 RISC-V 方向，所有作业阶段均以 RISC-V 作为唯一目标架构。

固定工具链和文件如下：

| 用途 | 固定选择 |
| --- | --- |
| C/C++ 交叉编译器 | `riscv64-unknown-elf-gcc` |
| RISC-V 汇编器 | `riscv64-unknown-elf-as` |
| RISC-V 链接器 | `riscv64-unknown-elf-ld` |
| LLVM IR 工具 | `clang`、`llc`、`lli`、`llvm-as`、`llvm-dis`、`opt` |
| SysY 静态运行库 | `lib/libsysy_riscv.a` |
| SysY 运行库头文件 | `lib/sylib.h` |
| QEMU 模拟器 | `qemu-riscv64` |
| 首选标准库 ABI | RISC-V 64 位、Double-Float ABI、静态链接 |
| 代码模型 | `medany` |

### 当前环境状态

- 系统：WSL2 + Ubuntu 24.04，x86-64 主机。
- RISC-V GCC：`riscv64-unknown-elf-gcc` 10.2.0，已经配置在用户 PATH 中。
- RISC-V Binutils：`riscv64-unknown-elf-as`、`ld`、`objdump`、`readelf` 等已经可用。
- 运行库链接器：原 SiFive GNU ld 2.35 无法合并 `libsysy_riscv.a` 的新 ISA 属性，已用户级配置 GNU binutils 2.42，路径为 `~/.local/opt/riscv-binutils-2.42`。
- RISC-V ABI：默认目录为 `rv64imafdc/lp64d`，与 `libsysy_riscv.a` 的属性一致。
- LLVM/Clang：LLVM 18.1.3，用户级安装目录为 `~/.local/opt/llvm-18`。
- LLVM 命令入口：`clang`、`clang++`、`llc`、`lli`、`llvm-as`、`llvm-dis`、`opt`、`ld.lld` 等已配置在 `~/.local/bin`。
- QEMU：`qemu-riscv64` 8.2.2，已经可用。
- SysY 运行库：已解压到项目的 `lib/` 目录，`lib/libsysy_riscv.a` 可供 RISC-V 链接使用。
- MLIR/进阶实验：已用户级安装 AscendNPU IR 1.1.0，虚拟环境为 `~/.venvs/ascendnpuir`。
- 构建工具：已配置 Ninja 1.13.2，满足 AscendNPU IR 对 Ninja >= 1.12 的要求。
- 昇腾运行环境：当前 WSL 缺少 `hivmc`、CANN、`ASCEND_HOME_PATH` 和 `/dev/davinci*`，只能完成 MLIR 中端 lowering，不能进行 NPU 上板运行。
- 报告排版：不在 WSL 内安装 LaTeX，统一使用 Overleaf 编辑并导出 PDF。

除课堂讲解或工具链对比等必要场景外，不以 ARM 或 x86-64 作为实验目标。实验的核心产物、编译命令、运行验证和报告结论均围绕 RISC-V 展开。

## 三、第一次作业目标

第一次作业属于总体大作业中的“预备工作 1”，主要内容包括：

1. 以斐波那契程序为例，研究语言处理系统的完整工作过程。
2. 研究 GCC 的预处理器、编译器各阶段、汇编器和链接器。
3. 设计 SysY 示例程序。
4. 编写与 SysY 示例等价的 LLVM IR 程序。
5. 编写与 SysY 示例等价的 RISC-V 汇编程序。
6. 链接 SysY 运行库并生成 RISC-V 目标程序。
7. 使用 `qemu-riscv64` 验证运行结果。
8. 可选探索 MLIR 中端和 AscendNPU IR 的逐层 lowering 过程。
9. 撰写符合科技论文格式的 PDF 调研报告。

## 四、小组协作原则

1. 两人共同确定实验方案、SysY 示例程序、输入输出格式和验收标准。
2. 两人共同参加报告结构设计、结果复核和最终定稿。
3. 每一项任务都必须有可检查的产物，例如源文件、汇编文件、目标文件、命令日志、运行输出或截图。
4. 两人互相检查对方的关键产物，避免“各自完成、互不了解”。
5. 所有实验数据必须来自实际命令和实际运行结果，不得虚构。
6. 参考公开代码或资料时，必须在代码注释和报告参考文献中注明来源。
7. 同一份最终报告由两人分别在小雅平台按要求提交。

## 五、第一次作业具体分工

### 5.1 基础要求第一部分：语言处理系统工作过程

#### 共同任务

- 共同确定斐波那契程序的功能、输入输出格式和测试数据。
- 共同确定使用 RISC-V 交叉 GCC 研究完整编译过程。
- 共同整理 `fib.c`、`fib.i`、`fib.s`、`fib.o` 和最终 RISC-V 可执行文件之间的关系。
- 共同完成过程分析、图表设计和报告复核。

#### 刘国全负责

- 研究预处理阶段：
  - 编写或整理斐波那契 C 源程序 `fib.c`。
  - 使用 `riscv64-unknown-elf-gcc -E` 生成 `fib.i`。
  - 分析头文件展开、宏替换、条件编译和注释删除等预处理结果。
- 研究 GCC 编译阶段：
  - 使用 `riscv64-unknown-elf-gcc -S` 生成 RISC-V 汇编 `fib.s`。
  - 说明词法分析、语法分析、语义分析、中间表示生成、优化和目标代码生成的基本作用。
  - 分析 GCC 如何从 C 源码得到 RISC-V 汇编。
- 负责整理该部分的命令、阶段产物和问题解释。

#### 刘昀皓负责

- 研究汇编阶段：
  - 使用 `riscv64-unknown-elf-gcc -c` 或 `riscv64-unknown-elf-as` 将 `fib.s` 转换为 RISC-V 目标文件 `fib.o`。
  - 分析汇编指令、符号表、重定位信息和 ELF 目标文件结构。
- 研究链接阶段：
  - 使用 `riscv64-unknown-elf-gcc` 链接目标文件和 `libsysy_riscv.a`。
  - 分析符号解析、重定位、运行库合并和可执行文件生成过程。
- 研究运行验证：
  - 使用 `qemu-riscv64` 运行生成的 RISC-V 程序。
  - 记录输入、输出、退出状态和命令结果。
- 负责整理汇编器、链接器和 QEMU 相关证据。

#### 交叉复核

- 刘昀皓检查刘国全生成的 `fib.i` 和 `fib.s`。
- 刘国全检查刘昀皓完成的 `fib.o`、链接命令和运行结果。
- 两人共同确认每一阶段产物之间的输入输出关系。

### 5.2 基础要求第二部分：SysY、LLVM IR 与 RISC-V 汇编

#### 共同任务

在开始各自实现之前，两人必须共同冻结以下内容：

- SysY 示例源码，例如 `sysy_demo.sy`。
- 程序功能及覆盖的语言特性。
- 输入格式和输出格式。
- 使用的 SysY 运行库函数。
- 测试用例和预期输出。
- 统一文件名、目录结构和编译链接方式。

#### SysY 示例程序的覆盖范围

基础版本至少覆盖：

- `int` 类型；
- 变量声明和赋值；
- 常量声明；
- 算术运算 `+`、`-`、`*`、`/`、`%`；
- 关系运算和逻辑运算；
- `if` 条件分支；
- `while` 循环；
- 函数定义和调用；
- `return` 语句；
- `getint`、`putint` 等运行库调用。

在时间和实现能力允许时，增加数组、浮点数等进阶特性。示例程序必须保持规模适中，确保 LLVM IR 和 RISC-V 汇编可以人工检查，并能清楚证明两者语义等价。

#### 刘国全负责

- 设计并说明 SysY 示例程序 `sysy_demo.sy`。
- 编写等价的 LLVM IR 程序，例如 `sysy_demo.ll`。
- 在 LLVM IR 中正确声明和调用 SysY 运行库函数。
- 使用 LLVM 工具完成 IR 检查或目标代码生成，例如：
  - `llvm-as`；
  - `llvm-dis`；
  - `llc`；
  - `lli`（作为辅助验证）。
- 将 LLVM IR 编译为 RISC-V 目标代码，并通过 RISC-V 工具链链接 `libsysy_riscv.a`。
- 使用 `qemu-riscv64` 验证运行结果。
- 记录 LLVM IR 生成、编译、链接和运行过程。

#### 刘昀皓负责

- 根据同一份 `sysy_demo.sy` 编写等价的 RISC-V 汇编程序，例如 `sysy_demo.s`。
- 保证函数调用约定、栈帧、参数传递、返回值和运行库调用符合 RISC-V ABI。
- 使用以下工具生成和检查目标文件：
  - `riscv64-unknown-elf-as`；
  - `riscv64-unknown-elf-objdump`；
  - `riscv64-unknown-elf-readelf`；
- 使用 `riscv64-unknown-elf-gcc` 链接 `libsysy_riscv.a`。
- 使用 `qemu-riscv64` 运行并记录结果。
- 整理 RISC-V 汇编、目标文件和 ELF 可执行文件之间的对应关系。

#### 交叉复核

- 两人使用完全相同的测试输入。
- 刘国全的 LLVM IR 路线和刘昀皓的 RISC-V 汇编路线必须产生相同输出。
- 至少测试正常输入、边界输入和异常或退化输入。
- 双方分别运行对方的结果，并确认结果可复现。
- 如果输出不一致，共同定位是 SysY 语义、ABI、运行库调用还是链接过程的问题。

### 5.3 进阶要求：观察 MLIR 中端处理过程

此项为可选加分项，两人共同完成。

#### 刘国全负责

- 阅读 MLIR 多层方言和渐进式 lowering 的资料。
- 梳理 MLIR 与传统 LLVM IR 单层中间表示的区别。
- 调研 AscendNPU IR 在 MLIR 生态中的层次和主要方言。
- 整理 lowering 过程中抽象层次逐步降低的路线。

#### 刘昀皓负责

- 查阅官方 VecAdd 快速入门示例。
- 整理环境配置步骤、示例结构和可复现命令。
- 记录执行过程中遇到的依赖、硬件或平台限制。
- 若无法运行昇腾硬件程序，则整理编译流程、文档依据和错误信息，不虚构运行结果。

#### 共同任务

- 比较 MLIR 与 LLVM IR 在中端表示和优化方面的差异。
- 比较 AscendNPU IR 中端路径与 RISC-V 后端路径的抽象层次差异。
- 共同完成进阶部分结论和报告撰写。
- 如果环境无法完整配置，则以“调研过程 + 可复现实验边界 + 原理分析”作为报告内容。

## 六、建议目录结构

```text
Compiler-Principles-2026/
├── docs/
│   ├── 实验分工.md
│   └── 报告/
├── first_assignment/
│   ├── part1_gcc/
│   │   ├── fib.c
│   │   ├── fib.i
│   │   ├── fib.s
│   │   ├── fib.o
│   │   └── logs/
│   ├── part2_sysy_llvm_riscv/
│   │   ├── sysy_demo.sy
│   │   ├── sysy_demo.ll
│   │   ├── sysy_demo.s
│   │   ├── expected/
│   │   ├── actual/
│   │   └── logs/
│   └── advanced_mlir/
│       ├── notes/
│       └── logs/
├── lib/
│   ├── libsysy_riscv.a
│   ├── sylib.c
│   └── sylib.h
└── README.md
```

实际目录可以在开始执行实验时再创建，但建议从一开始就保持源文件、生成文件和日志相互对应。

## 七、统一命名和接口约定

1. 输入输出统一使用文本格式。
2. 每个测试用例应有独立编号，例如 `case01_input.txt`、`case01_expected.txt`、`case01_actual.txt`。
3. 命令输出统一保存到对应 `logs/` 目录。
4. 源程序、可执行文件和日志不得互相覆盖。
5. 所有生成文件应写明生成命令，便于复现。
6. SysY 源程序不得包含头文件，所需运行库函数由编译器或手写 IR/汇编直接处理。
7. 运行库统一链接 `libsysy_riscv.a`，不链接 x86 或 AArch64 版本。

## 八、推荐执行流程

### 8.1 基础第一部分

1. 编写 `fib.c`。
2. 预处理生成 `fib.i`。
3. 编译生成 RISC-V 汇编 `fib.s`。
4. 汇编生成 RISC-V 目标文件 `fib.o`。
5. 链接生成 RISC-V 可执行文件。
6. 使用 `qemu-riscv64` 验证。
7. 分析各阶段产物的对应关系并截图。

### 8.2 基础第二部分

1. 两人共同冻结 `sysy_demo.sy` 和测试数据。
2. 刘国全编写 LLVM IR。
3. 刘昀皓编写 RISC-V 汇编。
4. 两人分别生成可执行文件。
5. 使用相同输入运行并比较结果。
6. 交换复核并通过报告记录差异。
7. 完成 LLVM IR 与 RISC-V 汇编的对照分析。

### 8.3 进阶部分

1. 阅读 MLIR、AscendNPU IR 和 VecAdd 官方资料。
2. 记录环境要求及当前 WSL 的限制。
3. 尝试 VecAdd 示例和 lowering 流程。
4. 整理方言层级、lowering 阶段和观察结果。
5. 将探索过程和限制写入报告。

## 九、验收标准

第一次作业完成时，至少应满足以下条件：

- 两人分工在报告中清楚说明。
- GCC 工作过程的每个阶段都有实际产物或命令证据。
- 第一部分的目标产物为 RISC-V。
- 存在同一份、版本冻结的 SysY 示例程序。
- LLVM IR 与 RISC-V 汇编均与 SysY 示例语义一致。
- 两者都能链接 `libsysy_riscv.a`。
- 两者都能生成 RISC-V 目标程序。
- 两者都能通过 `qemu-riscv64` 运行。
- 相同测试输入下输出一致。
- 报告包含题目、摘要、关键词、引言、工作与结果、结论和参考文献。
- 报告为 PDF，图文和表格规范。
- 两人均在小雅平台提交最终报告。

## 十、报告撰写分工

### 刘国全主要负责

- 摘要和引言的初稿。
- GCC 预处理器和编译器各阶段的原理及结果。
- SysY 示例设计说明。
- LLVM IR 相关内容。

### 刘昀皓主要负责

- GCC 汇编器和链接器部分。
- RISC-V 汇编与 ABI 分析。
- QEMU 运行验证和测试结果。
- 图表、日志整理。

### 两人共同完成

- 实验方案和结论。
- 数据核对和交叉验证。
- MLIR 进阶部分。
- 全文复核、格式检查和最终 PDF 导出。
- 小雅平台提交。

## 十一、进度和风险控制

### 关键依赖

- `riscv64-unknown-elf-gcc` 工具链。
- `qemu-riscv64`。
- LLVM/Clang 工具链。
- `libsysy_riscv.a`、`sylib.h` 和运行时库文档。
- `SysY2022语言定义-V1.pdf`。
- `SysY文法补充说明.pdf`。

### 主要风险

1. LLVM IR 与手写 RISC-V 汇编的调用约定不一致。
2. SysY 语言特性理解不一致，导致两种实现语义不同。
3. 链接参数、代码模型或 ABI 不一致。
4. 使用 C++ 输入输出代码代替 SysY 运行库调用。
5. 只关注运行结果，没有保存完整命令和中间产物。
6. 两人独立完成但缺乏交叉检查。
7. MLIR/AscendNPU IR 环境依赖复杂，无法进行硬件运行。
8. 参考公开代码但未注明来源。

### 应对措施

1. 先冻结 SysY 源程序和测试输入。
2. 先完成最小可运行示例，再增加语言特性。
3. 每次修改源代码后重新运行全部测试用例。
4. 每个阶段保留命令、输出和版本信息。
5. 使用 Git 记录修改历史。
6. MLIR 无法运行时，如实记录环境限制和文档调研结果。
7. 所有外部参考均在注释和参考文献中注明。

## 十二、最终提交约定

- 最终报告名称：`编译原理第一次上机作业_刘国全_刘昀皓.pdf`。
- 提交格式：PDF。
- 提交平台：小雅。
- 提交人：刘国全、刘昀皓均需提交。
- 报告内容：两人共同撰写，必须写明分工。
- 代码和日志：按教师或助教要求打包提交。
- 提交前由两人分别完整检查一次，确认版本一致。

## 十三、结论

本项目从第一次作业开始即固定使用 RISC-V 技术路线。刘国全主要负责预处理、编译器各阶段、SysY 设计和 LLVM IR；刘昀皓主要负责 RISC-V 汇编、汇编器、链接器和运行验证。进阶 MLIR 部分由两人共同探索。所有后续编译器模块均以生成 RISC-V 汇编和运行 RISC-V 目标程序为统一验收标准。
